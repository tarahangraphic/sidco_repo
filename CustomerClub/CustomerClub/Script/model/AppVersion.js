function AppVersion(id,versioncode,versionname,releasedate,url) {
    this.Id = id;  
    this.VersionCode = versioncode; 
    this.VersionName = versionname;
    this.ReleaseDate = releasedate;
    this.Url = url;
}